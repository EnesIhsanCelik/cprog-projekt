#include <string>
#include "CountHits.h"
#include "Engine.h"

namespace game {
    
    void funk(LaberPtr label, SpritePtr e);{
        if (txt == "Öka") {
            value++;
        }        
        label->setText(to_string(value));
    }
}